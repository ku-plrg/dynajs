# Everything under src/model/spec/ is regenerated by `npm run copy` and ignored
# by default, so `git status` stays free of generated noise. Hand-authored
# @manual files live here too but can't be told apart by name (all are AO__*.ts),
# so they are force-staged by content via `npm run stage:manual` (git add -f).
*
!.gitignore
